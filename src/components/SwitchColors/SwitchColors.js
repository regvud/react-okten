import React from 'react';
import styles from './SwitchColors.module.css'

const SwitchColors = ({color}) => {
    return (
        <div className={styles.color}>
            <h1>IVAN</h1>
            {/*<button onClick={() => switcher()}>Switch to GREEN</button>*/}
        </div>
    );
};

export default SwitchColors;
import React from 'react';

const CommentsComponent = ({comment}) => {
    // {postId, id, name, email, body} = comment
    return (
        <div>
        </div>
    );
};

export default CommentsComponent;